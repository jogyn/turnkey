# Optional: base URL for the app (e.g. for auth callbacks)
# SERVER_URL=http://localhost:3000

# Better Auth: secret for signing (min 32 chars). Required for production.
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=

# Optional: path to SQLite database. Defaults to ./data/auth.sqlite
# DATABASE_PATH=./data/auth.sqlite

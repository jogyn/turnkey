# Copy this file to .env.local and fill in values.
# .env.local is gitignored — never commit secrets.

# Optional: base URL for the app (e.g. for auth callbacks). Better Auth reads BETTER_AUTH_URL or set baseURL in config.
# BETTER_AUTH_URL=http://localhost:3000

# Better Auth: secret for signing (min 32 chars). Required for production.
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=

# Optional: path to SQLite database. Defaults to ./data/auth.sqlite
# DATABASE_PATH=./data/auth.sqlite

# Optional: comma-separated user IDs that can use admin (list users, set role, etc.). Add your user ID from the demo page.
# BETTER_AUTH_ADMIN_USER_IDS=
